windows:
  installs: winget install nilesoft.shell

  hooks:
    install:
      post: |
        shell -register -init
        shell -restart
    link:
      post: shell -restart

  links:
    shell.nss: C:\Program Files\Nilesoft Shell\shell.nss
